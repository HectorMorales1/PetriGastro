{
  "name": "petrigastro",
  "private": true,
  "scripts": {
    "build": "cd server && npm install",
    "start": "node server/server.js",
    "postinstall": "cd server && npm install"
  },
  "dependencies": {
    "nodemailer": "^8.0.7"
  }
}
}
